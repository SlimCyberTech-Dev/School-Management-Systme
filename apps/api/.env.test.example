# Local Jest integration tests (optional — defaults in tests/setup.ts work after setup:test-db)
DATABASE_URL_TEST=postgresql://test:test@localhost:5432/school_manage_test
# Must match DATABASE_URL_TEST so platformPool does not inherit Render PLATFORM_DATABASE_URL from .env
PLATFORM_DATABASE_URL_TEST=postgresql://test:test@localhost:5432/school_manage_test
